- Confirm the clarify->implement outcome tool name against tatara-operator's shipped MCP surface; today it reuses `issue_outcome` as a placeholder in tatara-mcp-scm-lifecycle and tatara-clarify-conversation (task-kind redesign, 2026-07-11).
- tatara-claude-code-wrapper needs an `installAgents`/`installAgentsFromSrc` path (mirroring `installSkills`) to actually flatten this repo's `.claude/agents/*.md` into a running pod; until it lands, explorer/tester/builder/architect are inert cargo (task-kind redesign, 2026-07-11).
- Confirm tatara-operator's scheduled Documentation trigger shape (cron cursor vs. this skill's git-log-derived "since when") and adjust tatara-documentation-workflow Step 1 if the operator injects an explicit cursor (task-kind redesign, 2026-07-11).
- Follow-up brainstorm-kind issue: tatara-deep-research's procedure (propose/comment on a platform improvement) is stale against its own `incident` profile tag - tatara-incident-sre never invokes it and does read-only RCA instead; reconcile what "deep research" should mean for incident once this plan lands (flagged, not fixed, in the redesign plan's Open risks).
