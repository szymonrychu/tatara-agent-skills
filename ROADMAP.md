- tatara-claude-code-wrapper needs an `installAgents`/`installAgentsFromSrc` path (mirroring `installSkills`) to actually flatten this repo's `.claude/agents/*.md` into a running pod; until it lands, explorer/tester/builder/architect are inert cargo (task-kind redesign, 2026-07-11).
- Confirm tatara-operator's scheduled Documentation trigger shape (cron cursor vs. this skill's git-log-derived "since when") and adjust tatara-documentation-workflow Step 1 if the operator injects an explicit cursor (task-kind redesign, 2026-07-11).
- `.pre-commit-config.yaml` does not exist in this repo. The task-centric skills plan names it as a Task 9 create-target with no specified contents; it never landed. Someone needs to author it (hooks: at minimum the two validators) before `pre-commit run --all-files` can be used here (task-centric MCP surface rewrite, 2026-07-13).
- `validate_skills.py`/`validate_profiles.py` never inspect an MCP tool call's param names or enum values against the cli's actual JSON schemas - only forge-CLI and merge-instruction violations and profile routing are checked. That is why 5 skill-vs-tool-surface drift bugs (invented `code_context`/`code_graph` params, an invalid `report_internal_issue` category) passed CI silently. Vendor the tatara-cli tool schemas into this repo (like the `testdata/agent-kinds.txt` golden) and add a validator that parses every documented tool call in skills/ against them (task-centric MCP surface rewrite, 2026-07-13).
- `check_vocabulary_currency.py`'s shrink guard is per-leaf ("lost more than half") plus an aggregate ("lost more than a quarter of all members"). Neither fires on ONE leaf losing, say, 6 of 17 - the shape `expand()` produces if a nested DTO gains a package qualifier and recursion stops. That bump reads as ordinary drift and is caught only by the `validate_vocabulary.py` run in the PR body plus the no-auto-merge rule. A structural check (assert every ROOT_STRUCT still recurses) would close it (#68, 2026-08-23).
