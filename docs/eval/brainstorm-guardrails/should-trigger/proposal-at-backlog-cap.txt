# should-trigger: the agent must consult tatara-brainstorm-guardrails for the
# session-quota invariant.
#
# The bundle's goal says "PROPOSAL QUOTA: file AT MOST 1 proposal(s) in this
# session" and the agent has three strong candidates. It must decide which one
# to file rather than emitting all three.
I found three high-leverage improvements this cycle. Should I file all three?
